<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/backs_blue</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <!-- 禁止显示窗体标题栏 -->
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimary">@color/title_bar</item>
        <item name="colorPrimaryDark">@color/title_bar</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <style name="NoDialogTitleView" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="TitleBar_Style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/title_bar_height</item>
        <item name="android:background">@color/title_bar</item>
    </style>

    <style name="TitleBar_TitleText_Style">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_centerInParent">true</item>
    </style>

    <style name="TitleBar_ComeBack">
        <item name="android:layout_centerVertical">true</item>
        <item name="android:src">@drawable/come_back_background</item>
        <item name="android:padding">@dimen/item_right_raw_paddingRight_Left</item>
        <item name="android:onClick">come_back</item>
    </style>

    <style name="MyDailog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>

    <!-- ActionSheet进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/popup_in</item>
        <item name="android:windowExitAnimation">@anim/popup_out</item>
    </style>

    <!-- 自定义Dialog主题 -->
    <style name="BottomDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowAnimationStyle">@style/BottomDialogAnimation</item>
    </style>

    <!-- 自定义Dialog动画 -->
    <style name="BottomDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/input_method_enter</item>
        <item name="android:windowExitAnimation">@anim/input_method_exit</item>
    </style>
    <style name="item_click_background">
        <item name="android:background">@drawable/item_click_background</item>
    </style>

    <!-- dialog动画的DialogActivity -->
    <style name="DialogActivityStyle" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- 全屏幕拉伸 -->
    <style name="layout_full">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- 横向分布 -->
    <style name="layout_horizontal" parent="layout_full">
        <item name="android:layout_width">0px</item>
    </style>

    <!-- 纵向分布 -->
    <style name="layout_vertical" parent="layout_full">
        <item name="android:layout_height">0px</item>
    </style>

    <style name="item_CheckBox_style">
        <item name="android:layout_width">14dp</item>
        <item name="android:layout_height">14dp</item>
        <item name="android:button">@null</item>
        <item name="android:background">@drawable/checkbox_background</item>
    </style>

    <style name="Normal_EditText_Search_style">
        <item name="android:layout_height">30dp</item>
        <item name="android:hint">请输入患者姓名/患者组名</item>
        <item name="android:textColorHint">@color/hint_textColor</item>
        <item name="android:background">@drawable/normal_search_edittext_background</item>
        <item name="android:drawableLeft">@drawable/normal_search_icon</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:textSize">13sp</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:singleLine">true</item>
        <item name="android:inputType">text</item>
        <item name="android:imeOptions">actionSearch</item>
    </style>

</resources>
